# Build-time environment variables for the static PWA.
# Copy to .env to enable for local builds; set in your hosting platform's env config for production.

# Cloudflare Web Analytics token (private — get from Cloudflare dashboard → Web Analytics).
# When set, the layout injects the CWA beacon script. When unset, no analytics load.
# PUBLIC_CF_ANALYTICS_TOKEN=
